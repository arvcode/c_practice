/* Rotate an array -Numbers[0-255] or characters */

/* made using ll & array*/
 
#include<stdio.h>

typedef struct ll {
	unsigned char ch;
	struct *ll next;	
}ll_t;


char * rotate_array(char *, int);


int main() {
	
	char *c;
	int length;
	printf("enter the length of string \n");
	scanf("%d",&length);
	c=(char)malloc(sizeof(char)*length*);

	scanf("%s",c);

	printf("enter the number of rotations \n");
	scanf("%d",&length);
	printf("%s",rotate(c,);	





	return 0;
}


